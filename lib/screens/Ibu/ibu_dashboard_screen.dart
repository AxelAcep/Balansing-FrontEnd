// lib/screens/kader_dashboard_screen.dart
import 'package:flutter/material.dart';

class IbuDashboardScreen extends StatelessWidget {
  const IbuDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Ini adalah halaman Dashboard Kader.',
        style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
      ),
    );
  }
}